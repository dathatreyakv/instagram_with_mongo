development:
  clients:
    default:
      database: instagram_development
      hosts:
        - localhost:27017
      options:

  options:

test:
  clients:
    default:
      database: instagram_with_mongo_test
      hosts:
        - localhost:27017
      options:
        read:
          mode: :primary
        max_pool_size: 1
